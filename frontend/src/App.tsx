function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">DocuParse</p>
        <h1>React frontend is ready.</h1>
        <p className="lead">
          Start editing <code>src/App.tsx</code> to build your interface.
        </p>
      </section>
    </main>
  )
}

export default App
